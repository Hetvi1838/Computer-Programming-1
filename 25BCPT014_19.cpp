#include<stdio.h>
int main()
{
    float r, a;
    printf("Enter the radius of the circle:");
    scanf("%f", &r);
    a=(22.0/7.0)*r*r;
    printf("The area of the circle is %f . ", a);
    return 0;
}
